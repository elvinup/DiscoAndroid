<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="CustomPreferencesTheme" parent="PreferenceThemeOverlay.v14">
        <item name="android:background">#FFFFFF</item>
        <item name="switchPreferenceCompatStyle">@style/CustomSwitchPreferencesTheme</item>
    </style>

    <style name="CustomSwitchPreferencesTheme" parent="Preference.SwitchPreferenceCompat">
        <item name="android:widgetLayout">@layout/custom_switch_compat</item>
        <item name="android:selectable">false</item>
    </style>

    <style name="CustomSwitchTheme">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:gravity">center_vertical</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/app_initials_white_background</item>
    </style>


</resources>
